# Rust / Cargo
/target
**/*.rs.bk
# Cargo.lock IS committed: Brindle is an end-product extension, so we want
# reproducible builds (CI, releases). (Only omit it for a published library crate.)

# pgrx
*.so
*.dylib
/sql/*--*.sql     # generated extension SQL (cargo pgrx generates these)
.pgrx/

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Keep AI working instructions out of the public repo
CLAUDE.md

# Project-management / orchestration (dev-only, never shipped to production)
/tasks/

# Architecture/design notes (dev-only for now; remove this line to publish)
/design/
